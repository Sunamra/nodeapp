#@ Number of digits

def sum_of_digits(num):
    temp = num
    total = 0

    while temp > 0:
        rem = temp % 10
        total += rem
        temp //= 10

    return total


number = int(input("Enter a number: "))

print("Sum of the digits: ", sum_of_digits(number))

