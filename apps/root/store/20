#@ Total Digits in a Number

read -p "Enter a number : " num
count=0
if [ $num -eq 0 ]; then
	count=1
else
	while [ $num -gt 0 ]; do
		num=$(expr $num / 10)
		count=$(expr $count + 1)
	done
fi
echo "Total number of digits : $count"
