#@ GCD of Two Numbers

def gcd(a, b):
    while b != 0:
        a, b = b, a % b
    return abs(a)

def gcd_recur(a, b):
    if b == 0:
        return abs(a)
    return gcd(b, a % b)


x, y = int(input("Enter Number 1: ")), int(input("Enter Number 2: "))

print("GCD: ",gcd(x, y))
