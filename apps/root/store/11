#@ First n Fibonacci Series (Recursion)

def fib(n):
    if n == 0:
        return 0
    elif n == 1:
        return 1
    else:
        return fib(n - 1) + fib(n - 2)


def print_fib(n):
    for i in range(n):
        print(fib(i), end=" ")


print_fib(int(input("Enter Range: ")))
