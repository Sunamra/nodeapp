{
	"name": "express-app",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"start": "npm i && node index.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"push": "git add . && git commit --allow-empty-message && git push",
		"pull": "pm2 stop 0 && git pull && pm2 start 0"
	},
	"keywords": [ ],
	"author": "Sunamra Das",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"chalk": "^4.1.2",
		"cors": "^2.8.5",
		"express": "^5.1.0",
		"multer": "^1.4.5-lts.2"
	}
}
